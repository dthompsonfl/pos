<?xml version="1.0" encoding="utf-8"?>
<!-- POS data contains payment records, employee PIN hashes, audit logs, and customer PII.
     Backup is DISABLED entirely (android:allowBackup="false" in manifest).
     This file is a defense-in-depth: even if allowBackup is somehow enabled, no data is extracted. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
