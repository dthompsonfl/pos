<?xml version="1.0" encoding="utf-8"?>
<!-- Network security config — enforces HTTPS for all production traffic.
     Debug builds permit cleartext for local backend development only. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
